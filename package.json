{
  "name": "cgs-esg-momentum-engine",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run kill-port && npm run dev",
    "dev:frontend": "cd frontend && npm run kill-port && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
